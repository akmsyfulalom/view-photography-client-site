import React from 'react';
import useTitle from '../../Hooks/Hooks';

const Blog = () => {
    useTitle('Blog')
    return (
        <div>
            <h1>hello</h1>
        </div>
    );
};

export default Blog;